export default class MinutesTimer {
    timer: number;
    display: string;
    counterState: 'created' | 'started' | 'resumed' | 'paused';
    private addTime;
    private startDate;
    private pausedTime;
    private minutes;
    private seconds;
    private timerId;
    constructor(startTime?: string | number);
    addSeconds(timeInSeconds: number): void;
    stopAndChangeTime(timeInSeconds?: string | number): void;
    getTime(): this;
    resume(): void;
    start(): void;
    pause(): void;
    private countUp;
    private displayMinAndSeconds;
    private getSecondsFromStringTime;
}
